import React from 'react';

const Strike = ({ strike }) => {
  return (
    <div className={`strike ${strike}`}>
      {/* Your visual representation of the winning line here */}
    </div>
  );
};

export default Strike;